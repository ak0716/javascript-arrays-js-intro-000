var chocolateBars = ['snickers', 'hundred grand', 'kitkat', 'skittles']

var myArray = [1];
addElementToBeginningOfArray = (myArray, n) => {
  myArray.unshift('foo');
}

addElementToBeginningOfArray()
